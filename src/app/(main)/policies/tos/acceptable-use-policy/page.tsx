import { AupText } from "../_components/policiesText";
import { SectionWrapper } from "@/app/_components/global/Wrapper";

export default function Aup() {
  return (
    <SectionWrapper id="aup">
      <AupText />
    </SectionWrapper>
  );
}
