import { SlaText } from "../_components/policiesText";
import { SectionWrapper } from "@/app/_components/global/Wrapper";

export default function Sla() {
  return (
    <SectionWrapper id="Sla">
      <SlaText />
    </SectionWrapper>
  );
}
